# apps/guojin/env.yaml.example  —— **严格路径 operator_attested** 的样例（仅将来测真账户/生产才需）。
# ★团队内自测无需这个：现行 apps/guojin/env.yaml 用轻量档 assurance_level=trusted_internal，声明即解锁，无需签名。
# 下面这套(operator_attested + 签名)是休眠的严格路径，测真账户时再照此填 env.yaml 并签名。
# 供负责人照抄的"填好"样例（此文件仅示例，不被执行流读取）。
# ★用假名/假日期，绝不冒用负责人真实身份签认证（spec §4.6/§D5 执行程序不得自签/伪造）。
# ★此样例 attested_by 为占位名（EXAMPLE-OPERATOR）：即便你照这形状把日期/版本都填对，
#   verify_env 也会判 attestation_placeholder 回退 confirm_only——占位署名达不了 simulated_submit。
#   真实认证必须在 apps/guojin/env.yaml 用真实署署人名（如 ops-shenjie），不是照抄这里的占位名。
# 负责人照此结构填写 apps/guojin/env.yaml 的真实值，再用 .secrets 密钥 env_sign 生成 .sig。
type: simulation
assurance_level: operator_attested
evidence:
  attested_by: EXAMPLE-OPERATOR        # 样例占位假名——真实认证请填到 env.yaml 且用真名
  attested_at: "2026-01-01"            # 样例假日期
  valid_until: "2099-12-31"            # 样例假日期(仅为让样例通过 verify_env 形状校验)
  basis: ["【样例】项目运营约定仅使用国金模拟盘", "【样例】账户 pt/xy 为指定测试账户"]
  package: com.hexin.plat.android.GuoJinZXGSecurity
  version_range: {min: "8.05.001", max_exclusive: "8.06.000"}
  account_aliases: [pt, xy]
revoked: false
