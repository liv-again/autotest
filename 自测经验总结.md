# AI 驱动 Android App 做 Excel 用例自测 — 经验总结与 Skill 评估

> 面向：后续继续跑这套（国金证券App/北交所ETF用例），也给"以后换个app/换批用例"复用。
> 沉淀自 2026-07-28 几批实跑（详情页/买入/大宗/全场景扫）。

---

## 一、已跑通的工作流（照这个来最省心）

```
解析Excel(分档) → 按"屏"分组用例 → 串行驱动手机(一屏多用例) → 元素树断言 + 截图取证 → 回填tested字典 → 重生成标注Excel → 记性价比
```

**工具（都在 `tools/`）**
- `droid.py`：adb 驱动助手。子命令 `current/screen/find/has/tap/type/key/swipe/shot`。
  - `has "关键词"` → 退出码判断某文本是否在当前屏（**断言首选**，不用肉眼读乱码）。
  - `find "关键词"` → 打印匹配节点+坐标（定位可点元素）。
  - `tap --id/--text/--xy`、`type`（纯数字/ASCII可，**中文不行**）、`swipe x1 y1 x2 y2`、`shot 路径`。
- `annotate_excel.py`：把结果写进 `tested` 字典→重跑→在原Excel右侧追加 AI 列 + **内联截图**。
- `metrics.py`：`now`(打UTC时间戳) / `tokens SINCE UNTIL`(按会话transcript算token与$)。
- `battleplan.py`（scratchpad）：从Excel提"现在能跑的去重用例"，按 sheet 分组 + 标 green/data/intraday。

---

## 二、血泪坑 & 解法（**这些最值钱，务必先看**）

| 坑 | 现象 | 解法 |
|---|---|---|
| **终端中文乱码** | bash 里 `screen` 输出是 GBK 乱码，没法读值 | 断言用 `has`/`find`（内部比对，不看输出）；**要读实际值就 `PYTHONUTF8=1 python droid.py screen > 文件` 再用 Read 工具读**（UTF-8正常） |
| **重定向崩溃** | `screen > file` 报 `gbk codec can't encode` | 同上，命令前加 `PYTHONUTF8=1` |
| **Git Bash 改写 /sdcard 路径** | 截图/dump 路径被改成 `C:/Program Files/Git/sdcard/...` | droid.py 已用 Python subprocess 参数列表规避；手写命令加 `MSYS_NO_PATHCONV=1` |
| **FLAG_SECURE 挡截图** | `screencap` 得 0 字节 | 券商App默认开；**已由用户关掉→截图恢复(每张~200-500KB)**。若又变0字节先查这个 |
| **坐标随交互漂移** | 选中标的后价格±按钮从 x=103 变到 x=218/529 | **每次交互后重新 `find` 取坐标**，别用上一屏的老坐标连点 |
| **输代码自动跳转/自动补全** | `type 950025` 直接进详情页 / 弹下拉 | 详情页搜索：整码会自动进详情；交易页：弹下拉需**再点选那一行**(如 625,713)才回填 |
| **数字键盘挡下半屏** | 输入后 dump 少一半元素/`find`空 | 输完先 `key back` 收键盘再 dump |
| **同名"更多"按钮** | 底部"更多"给的是页级菜单，不是图表工具栏 | 认准 resource-id / 位置；一个入口试≤2次找不到就标"入口待确认"跳过 |
| **图形渲染元素取不到** | T+0/京基icon/跟踪指数/走势线 元素树里 `find` 不到 | 这类是canvas/图片→**改用截图肉眼确认**（正好符合"我方只需驱动+截图"的职责） |

---

## 三、分档口径（客户端自测，**已与用户确认**）

- 只用两个降级判据：① 需**测试数据**(某状态标的/权限/持仓/历史) ② 需**交易时段/盘中**。命中→🟡；否则→🟢(⬜=iOS)。
- **"与自营/两端一致""走势图视觉正确"不作降级项**——那是测试团队的核对，我方只要**驱动到位+有数据+有截图**就算🟢通过。
- 全表分布（753行）：🟢33% / 🟡64% / ⬜；high 389行 🟢39%/🟡60%。

---

## 四、入口地图（**下次直接照这个导航，别再摸索**）

- **行情详情页(最肥集群)**：行情→顶部🔍搜 `950025`(整码自动进详情)。一屏可验：标签(T+0)/底部新闻·盘口tab/新闻数据/盘口字段/五档。
  - **简报价面板**(⭐入口已确认)：**点『报价头』(上方现价/高/低/开数据区,≈630,300)** → 弹出简报价面板：涨停/跌停/外盘/内盘/振幅/总手/量比/委比/申购状态/赎回状态/做市商数量/**证券简称(扩)=扩位简称**。再点一次收起。(跟踪指数不在此12字段内)
  - 分时**快捷设置**：图表右上⚙(≈1192,624) → IOPV线开关/买卖盘/量柱/全部设置。
  - **日K设置**：先点"日K"tab再点⚙ →里面也有IOPV开关（**用户确认非缺陷**：Android不区分周期，日K/分时共用IOPV设置面板）。
  - **IOPV字段显示需净值**：IOPV开关/联动/设置✅；但走势图上方IOPV字段(实时参考净值(IOPV)/溢价率)需标的有**实时净值行情**才渲染，北证测试标的950025无净值feed(五档近乎无深度)→字段不显示→这类归🟡需数据(测试团队用正式净值标的核)。
  - **ETF列表**：行情首页基金版块→ETF标签(≈1027,1225)→ETF列表；**北证ETF在列表内**(北证50ETF测试3/950002，按涨幅排序即置顶)，点条目跳转详情✓。**横屏**：`adb shell settings put system accelerometer_rotation 0; user_rotation 1`(横屏)，完事复位`user_rotation 0; accelerometer_rotation 1`。
  - K线区"更多▼"(≈1008,624)=分钟周期(120/60/30/15/5/1分)，**不是**筹码分布。
  - **下单**：详情页底部『下单』(购物车,≈483,2724) → 弹菜单 闪电买入/闪电卖出/撤单查询/**去交易下单**(→普通买卖) → 代码自动带入。
- **搜索京基icon**：顶部🔍→输部分码，下拉/历史列表每条带【京基】/【沪A】icon。
- **ETF列表**：行情首页"基金"版块→"ETF"标签(≈1027,1225)→ETF列表(点表头可排序)。
- **自选页顶部指数**：自选页面顶部显示指数条，点击可弹指数分时图弹框。**显示规则**：沪=上证指数、深=深证成指、北=北证50、科=科创综指、创=创业板指。弹框内指数列表按此顺序排列，可左右切换查看各指数分时图。
- **交易模块**：底部"交易"tab(≈787,2758)。含 买/卖/撤/持仓、当日委托/成交/综合查询、新股申购、**新三板交易**、**大宗交易>**、盘后固定价格。
  - **新三板买入**：新三板交易→买入→输`950025`(接受北证ETF!)→点下拉选中→价格/涨跌停/可买/持仓列表齐全。价格±按钮、数量±按钮、全仓/半仓/⅓/¼。
  - **大宗定价买入**：大宗交易→(风险揭示书点确定)→定价买入→输`950025`选中→买入价±0.001、可买量、五档、**持仓列表(滚动找950xx)**；点持仓行→代码回填。注意此页**买入量无+/-、无仓位控件**。
- **已知不支持(国金版本,非缺陷,用户确认)**：①大宗交易数量微调/仓位键;②跟踪指数(简报价12字段内也无);③**闪电下单不支持两融交易+市价委托**(仅限价快速下单);④**新三板交易模块内无大宗功能**(不是到大宗交易模块委托→新三板大宗用例全部不测);⑤**融资融券(两融)tab内无大宗入口**(两融大宗跳过);⑥**担保品买入/融资买入页无仓位键**(全仓/半仓/⅓/¼,页面+键盘均无;国金老版本设计,用户确认非缺陷;数量微调正常)。
- **融资融券(两融)入口**：交易tab顶部『融资融券』子tab(`tv_tab_rzrq`,需信用账号)→首页菜单 担保品买入/融资买入/买券还券/撤单·担保品卖出/融券卖出/卖券还款/查询·转账/划转/展期/盘后固定/资产负债·新股申购/直接还券/直接还款。买卖页与普通交易**同构复用同批id**(auto_stockcode/stockprice/dieting·zhangtingprice/stockvolume/couldbuy/btn_xianjia_weituo→市价『五档即成剩撤』/btn_buy);卖出/还款页默认展示`我的持仓`列表(选行回填后**别按back**)。确认框字段(股东***2927/…/信用买入·卖出委托),结果『委托成功,合同号X』。**坑**:担保品买入无仓位键;融资买入需标的池成员(950025非融资标的→名称空);划转需普通账号二次登录。跌停买/涨停卖零残留策略同样适用。
- **闪电下单入口**：个股详情页底部『下单』(购物车,≈483,2800)→弹菜单闪电买入/闪电卖出/撤单查询/去交易下单→点闪电买入/卖出弹**紧凑覆盖面板**(价格±/数量±/仓位键全仓~1/4仓/买入·取消);**此面板+下单确认框是独立window,uiautomator dump不到→用截图坐标驱动**(displayed×1.42=real)。撤单查询也在此菜单。
- **新三板交易入口**：交易tab→新三板交易→买入/卖出/撤单/持仓/委托查询/成交查询;买入页代码框输码后**点键盘『确定』或下拉stock_code_tv选中**才回填;确认框『股转买入/卖出确认』;申报类型『集合竞价+连续竞价』。**新三板市价委托**:输入代码后**点价格旁『限价』按钮→弹切换框→选市价买入/市价卖出**(同普通市价有[63600无效保护价]问题,需有盘口深度标的)。
- **入口待人工**：底部筹码分布工具栏(难描述,空着);认购期标的;无权限标的;闪电持仓列表展开按钮(国金紧凑面板未见)。ETF列表北证ETF条目**已定位**(950002涨幅排序置顶)。
- **⭐ 盘中下单链已跑通(重要教训)**：模拟盘盘中限价买入950025→确认框字段全对→**『委托已提交,合同号2』**→撤单列表见已报→撤单确认框→**『撤单已提交』**,全链路正常。**坑**:早前一次"当日委托查不到"是**AI操作过慢触发客户端『请勿重复提交委托请求』防重锁**,委托没受理。**解法:下单提交要连贯——在同一个bash调用里 `tap 买入按钮 → sleep 1.5 → tap 确认买入`,中间别夹截图/dump**;否则防重锁触发委托作废。下单类确定性强,最宜固化Maestro。

---

## 五、性价比经验（怎么把成本压下来）

- **一屏多用例**：进一次详情页→连续验证该屏所有用例，比逐例导航省数倍。本批 output-only 单用例降到 **~$0.41**（早期逐例 $1.3~2.0）。
- **output token 才是干净指标**；`cache_read`(上下文税) 随会话变长猛涨——本会话超长导致含税$≈3.9×。**要看真实单价：新开精简会话跑**，或**固化成脚本**（见下）。
- 截图别乱截：每个用例留 1 张最能说明问题的即可（内联进Excel）。

---

## 六、Skill 评估：**建议做，但拆成"通用流程" + "本项目适配"两层**

**适合固化的理由**：这套流程是**可复用的方法论**（分档口径、按屏分组、droid断言+截图、乱码解法、annotate回填、metrics度量），换app/换用例都用得上。

**建议的 skill 形态**（名字暂拟 `android-excel-uitest`）：
- **触发**：用户给一份Excel用例 + 一个Android app，要AI驱动跑业务自测。
- **skill 内容(通用)**：①环境自检(adb/PYTHONUTF8/FLAG_SECURE) ②分档口径(第三节) ③工作流(第一节) ④坑清单(第二节) ⑤度量方法(第五节) ⑥"入口不明≤2次即跳过"纪律。
- **随skill附带脚本**：`droid.py`/`annotate_excel.py`/`metrics.py`/`battleplan.py`（已就绪，参数化SRC/OUT即可）。
- **不进skill(项目特有)**：具体坐标、入口地图、标的代码——这些放项目 memory / 本文件。

**不适合直接固化的部分**：设备坐标、app专属入口——会漂移，靠运行时 `find` 动态取。

**下一步(等你拍板)**：
- 若要，我可以把本文件的通用部分抽成 `.claude/skills/android-excel-uitest/SKILL.md` + 把三个脚本参数化随skill分发。
- 更进一步：把已 PASS 的稳定用例固化成 **Maestro** flow（YAML），CI 里无AI也能回归——这是"实时跑通→固化成脚本"的终点，也是最省钱的回归方式。

**✅ 已产出（2026-07-29）**：
- `maestro/` — **下单/撤单链 6 条 flow**（普通买/卖、闪电买/卖、新三板买/卖）+ README。用 resource-id 选择器 + copyTextFrom 取跌停/涨停价保证挂单不成交可撤、账户零残留；闪电两条因面板 canvas 无 id 用坐标(设备相关)。
- `profiles/国金证券-券商画像.md` — 功能支持矩阵 / 入口地图(含 resource-id) / 交互坑 / 已验证链路 / 账户环境。**后续对国金做别的需求，照这份导航，不用再摸链路。**
- `profiles/测试数据代码需求清单.md` — 按"标的必须具备的属性"列这套用例需要的代码(有净值/有持仓/有盘口深度/融类/认购期/无权限/L2…)+国金环境用码+阻塞场景+换券商备码检查清单。**别家券商用这套用例前照此提前备码，一次跑通。**

---

## 七、明天盘中要补的（93条，重点）
下单/撤单/成交/委托提交/闪电下单/依赖历史查询。开盘(9:30-15:00)后：先造几笔成交→解锁"当日委托/成交/撤单/查询"链路，再跑闪电下单与两融(注意融类北证ETF无标的)。
